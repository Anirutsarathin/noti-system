# WaterMonitor Alert API (Express + MySQL)

## Run
```bash
cd backend
npm install
npm run dev
```

## ENV
สร้างไฟล์ `.env`:
```
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=yourpass
DB_NAME=horiba
PORT=4500
```

## Schema
รันไฟล์ `../sql/schema_alert_profile.sql`
